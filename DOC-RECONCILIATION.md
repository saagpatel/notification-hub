# DOC-RECONCILIATION.md

This file was generated by a `/doc-truth-up` documentation-reconciliation pass. The pass treats
the code as read-only ground truth and edits only documentation so it accurately reflects the
repo's actual current state. No code was changed, no builds or tests were executed, and no
commands were run other than `git branch`, `git rev-parse HEAD`, and `date`. All conclusions
are derived from reading source files, manifests, test files, and git history.

---

## Per-Claim Findings

### 1. What It Is

**Status:** `consistent`

**Evidence:** `pyproject.toml:3` ("Unified notification daemon for Claude Code, Codex, and
Claude.ai"), `src/notification_hub/server.py` (FastAPI app wiring), `README.md` intro paragraph.
The README accurately describes the project as a localhost-only HTTP event intake daemon that
classifies urgency, suppresses noise, and routes to JSONL log, Slack, and macOS push.

**Minor drift fixed:** `README.md` Docs section did not list `docs/PRODUCT-BOUNDARY.md`, which is
already cross-referenced in the body at line 242 ("See `docs/PRODUCT-BOUNDARY.md` for the current
ownership split"). The entry was added to the Docs nav section for completeness.

- Before: `docs/PRODUCT-BOUNDARY.md` absent from `## Docs` section
- After: added `- \`docs/PRODUCT-BOUNDARY.md\`: notification-hub, personal-ops, and bridge-db ownership split`

---

### 2. Current State

**Status:** `drifted` (one fix applied; one out-of-scope drift flagged for manual review below)

**Fixed — docs/CURRENT-STATE.md "Verified Baseline" test count:**

`docs/CURRENT-STATE.md` line 754 (in the "Expected current outcome" block of the Verified Baseline
section) said `pytest: 370 passed`. The 2026-05-12 session update within the same document
explicitly states "376 tests passing (up from 370)" and the 2026-05-16 section confirms "Root test
suite: 376 passed." The HANDOFF.md for 2026-05-30 additionally lists `tests/test_logs_burn_in_diagnostics.py`
and `tests/test_review_endpoints.py` as files changed, indicating the test suite grew further after
376. The "370" baseline was demonstrably stale.

- Before: `- \`pytest\`: 370 passed`
- After: `- \`pytest\`: ≥376 passed (376 confirmed as of 2026-05-12 session; current suite may be higher)`

**Cannot fix — mcp_server/README.md tool inventory:**

`mcp_server/README.md` was auto-generated by mcpforge and describes a single tool named `echo`.
The actual `mcp_server/server.py` implements 7 real named tools:
`post_event`, `get_health`, `get_health_details`, `get_review_data`,
`get_coordination_readiness`, `get_import_queue`, and `get_burn_in_reports`.
This is a significant mismatch — `echo` does not exist in the server and serves no purpose.
`mcp_server/README.md` is outside the allowed edit paths (`README.md`, `CLAUDE.md`, `AGENTS.md`,
`docs/`), so it is flagged here for human correction.

---

### 3. Stack

**Status:** `consistent`

**Evidence:** `pyproject.toml` confirms FastAPI (`>=0.136.1`), uvicorn (`>=0.48.0`), watchdog
(`>=6.0.0`), httpx (`>=0.28.0`), pydantic (`>=2.13.4`), Python `>=3.12`, build backend hatchling.
`pyproject.toml:68` sets `pythonVersion = "3.12"` for pyright. All matches README stack claims and
CLAUDE.md portfolio context block.

---

### 4. How to Run

**Status:** `consistent`

**Evidence:** `pyproject.toml` defines `packages = ["src/notification_hub"]` and the script
entry `notification_hub.server:app` is consistent with the documented `uvicorn` invocation.
`pyproject.toml` dev dependency group contains pytest, pyright, ruff — matching the Verification
section commands. `mcp_server/pyproject.toml:16` confirms `testpaths = ["."]`, consistent with
the documented `uv run --directory mcp_server --frozen pytest`.

---

### 5. Known Risks

**Status:** `consistent`

**Evidence:** CLAUDE.md "Known Risks" and `docs/PRODUCT-BOUNDARY.md` "Non-Goals" both correctly
reflect the code: no apply behavior in source (`operations.py`, `server.py` review endpoints all
return `applied: false`); localhost-only (`config.py` default host `127.0.0.1`); personal-ops
promotion is operator-mediated (no outbound calls in notification-hub source to personal-ops APIs).

---

### 6. Next Move

**Status:** `unverifiable`

**Reason:** The active backlog items in `docs/CURRENT-STATE.md` and `HANDOFF.md` are
forward-looking operator decisions (synthetic mail repeat investigation, first-rich proof gate
usage, ADR 0001 recheck). They cannot be confirmed or denied from reading code.

---

## Contradictions for Manual Review

The following drift exists in a file outside the allowed edit paths. A human must make this fix.

### `mcp_server/README.md` — tool inventory is entirely stale

**File:** `mcp_server/README.md:8–24`

**Problem:** The README was auto-generated by mcpforge and lists a single placeholder `echo` tool
that no longer exists in `mcp_server/server.py`. The actual server defines 7 tools:

| Tool | Description |
|---|---|
| `post_event` | Push an event into notification-hub |
| `get_health` | Return the daemon's basic health status |
| `get_health_details` | Return detailed health snapshot |
| `get_review_data` | Return operator review JSON |
| `get_coordination_readiness` | Return coordination-readiness summary |
| `get_import_queue` | Return the current import queue |
| `get_burn_in_reports` | Return available burn-in reports |

**One-line fix:** Replace the entire `### Tools` section (including the `echo` entry) with the
seven tools above, each with their `mcp_server/server.py` docstring as the description. The mcpforge
scaffold text ("Generated by mcpforge", setup prerequisites) can be retained unchanged.

---

## Footer

Generated: 2026-05-30 21:22:34 PDT  
Branch: `docs/truth-up-2026-05-30`  
HEAD reconciled against: `b611a37b6eb28e2a3baa385da921f4517519f520`
