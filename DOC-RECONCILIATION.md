# DOC-RECONCILIATION.md

This file was generated by a `/doc-truth-up` documentation-reconciliation pass. The pass treats
the code as read-only ground truth and edits only documentation so it accurately reflects the
repo's actual current state. No code was changed, no builds or tests were executed, and no
commands were run other than `git branch`, `git rev-parse HEAD`, and `date`. All conclusions
are derived from reading source files, manifests, test files, and git history.

---

## Per-Claim Findings

### 1. What It Is

**Status:** `consistent`

**Evidence:** `pyproject.toml:3` ("Unified notification daemon for Claude Code, Codex, and
Claude.ai"), `src/notification_hub/server.py` (FastAPI app), `README.md` intro paragraph.
The README accurately describes the project as a localhost-only HTTP event intake daemon that
classifies urgency, suppresses noise, and routes to JSONL log, Slack, and macOS push.

---

### 2. Current State

**Status:** `drifted` (two fixes applied; one out-of-scope drift flagged for manual review below)

**Fixed — README.md missing `action-export-retention` command:**

`pyproject.toml:44` defines `notification-hub-action-export-retention =
"notification_hub.cli:action_export_retention_main"`. `src/notification_hub/cli_parser.py:462–485`
confirms the subcommand with `--keep`, `--apply`, `--export-dir`, and `--json` flags. `HANDOFF.md`
confirms it shipped in PR #37. `docs/CURRENT-STATE.md:327` describes it ("Action-export retention
— new CLI + script entry mirrors `operator-review-session-retention`"). The command was entirely
absent from README.md's Operator Commands section and runtime diagnostics list.

- Before: no `action-export-retention` entry anywhere in README.md
- After: added three command examples to the Operator Commands code block, added a description
  paragraph between operator-review-session-retention and operator-handoff-drill, and added two
  `--frozen` variants to the runtime diagnostics list

**Fixed — docs/CURRENT-STATE.md "Safest Next Step" stale backlog:**

The "Safest Next Step" section listed four backlog items from the 2026-05-16 session and referenced
commit SHA `26780cd`. Reading the session updates above that section and the code confirms all four
items are complete: `action-export-retention` exists in `pyproject.toml:44` and
`cli_parser.py:462`; `near_rollup_singles` is implemented in `src/notification_hub/operations.py`
and surfaced in `src/notification_hub/models.py`; mcp_server smoke tests exist in
`mcp_server/tests/test_server.py`; the aa8fd718 draft cleanup is described as complete in the
2026-05-17 session update. The SHA `26780cd` predates the current HEAD.

- Before: "Start future work from `main` (`26780cd`). … Work in priority order:
  action-export retention → single-event visibility → mcp_server smoke test → aa8fd718 draft cleanup."
- After: section now points to the 2026-05-31 active backlog and notes all 2026-05-16 items complete

**Cannot fix — mcp_server/README.md tool inventory (same issue from prior pass):**

`mcp_server/README.md` was auto-generated by mcpforge and still describes a single placeholder
`echo` tool. The actual `mcp_server/server.py` implements 7 real named tools (unchanged since
the prior reconciliation): `post_event`, `get_health`, `get_health_details`, `get_review_data`,
`get_coordination_readiness`, `get_import_queue`, and `get_burn_in_reports`. This file is outside
the allowed edit paths, so it is again flagged for manual correction below.

---

### 3. Stack

**Status:** `consistent`

**Evidence:** `pyproject.toml` confirms FastAPI (`>=0.136.1`), uvicorn (`>=0.48.0`), watchdog
(`>=6.0.0`), httpx (`>=0.28.0`), pydantic (`>=2.13.4`), Python `>=3.12`, build backend hatchling.
`pyproject.toml:68` sets `pythonVersion = "3.12"` for pyright. All match README stack claims and
CLAUDE.md portfolio context block. No new dependencies added since the prior reconciliation.

---

### 4. How to Run

**Status:** `consistent` (after the action-export-retention fix above)

**Evidence:** `pyproject.toml:61` (`packages = ["src/notification_hub"]`) and the documented
`uvicorn notification_hub.server:app` invocation match. `pyproject.toml` dev dependency group
contains pytest, pyright, ruff — matching the Verification section commands. The script entry
`notification_hub.cli:main` is the dispatcher for all `notification-hub <subcommand>` invocations,
consistent with the documented operator commands. `mcp_server/pyproject.toml:16` confirms
`testpaths = ["."]`, consistent with the documented `uv run --directory mcp_server --frozen pytest`.

---

### 5. Known Risks

**Status:** `consistent`

**Evidence:** CLAUDE.md "Known Risks" and `docs/PRODUCT-BOUNDARY.md` "Non-Goals" correctly
reflect the code: no apply behavior (`operations.py`, `server.py` review endpoints all return
`applied: false`); localhost-only (`config.py` default host `127.0.0.1`); personal-ops promotion
is operator-mediated (no outbound calls in notification-hub source to personal-ops APIs).

---

### 6. Next Move

**Status:** `unverifiable`

**Reason:** The active backlog in `docs/CURRENT-STATE.md` (2026-05-31 session update) lists
forward-looking operator decisions: observing synthetic mail workflow repeats, using the First Rich
Proof Gate on the next rich proposal, and resolving ADR 0001. These cannot be confirmed or denied
by reading code.

---

## Contradictions for Manual Review

The following drift exists in a file outside the allowed edit paths. A human must make this fix.

### `mcp_server/README.md` — tool inventory still entirely stale (second flag)

**File:** `mcp_server/README.md:8–24`

**Problem:** The README was auto-generated by mcpforge and lists a single placeholder `echo` tool
that does not exist in `mcp_server/server.py`. This was flagged in the prior reconciliation
(`b611a37`) and remains unaddressed. The actual server defines 7 tools:

| Tool | Description |
|---|---|
| `post_event` | Push an event into notification-hub |
| `get_health` | Return the daemon's basic health status |
| `get_health_details` | Return detailed health snapshot |
| `get_review_data` | Return operator review JSON |
| `get_coordination_readiness` | Return coordination-readiness summary |
| `get_import_queue` | Return the current import queue |
| `get_burn_in_reports` | Return available burn-in reports |

**One-line fix:** Replace the entire `### Tools` section (including the `echo` entry) with the
seven tools above, each with their `mcp_server/server.py` docstring as the description.

---

## Footer

Generated: 2026-06-02 19:38:41 PDT  
Branch: `docs/truth-up-2026-06-02`  
HEAD reconciled against: `c740fa55d7ffe6dad82a60aba1b76d70f9b23cde`
